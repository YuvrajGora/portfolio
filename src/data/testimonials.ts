export interface Testimonial {
  name: string;
  role: string;
  quote: string;
}

// Testimonials section renders nothing until this has entries — add real
// testimonials here when you have them, and it appears automatically.
export const testimonials: Testimonial[] = [];
